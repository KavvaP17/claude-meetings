{
  "permissions": {
    "allow": [
      "Edit(*)",
      "Write(*)",
      "Bash(pnpm test*)",
      "Bash(pnpm --filter server test*)",
      "Bash(pnpm --filter client test*)",
      "Bash(git *)",
      "Bash(gh *)",
      "Bash(npx prisma *)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node .claude/hooks/stop.js"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "grep -oP '(?<=\"file_path\":\")[^\"]*' | while IFS= read -r f; do [ -n \"$f\" ] && npx --yes prettier --write --ignore-unknown \"$f\"; done",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
